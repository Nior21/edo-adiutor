// Помощник для ЭДО — внешняя обработка (React UI + API ЭТН).

#Область ПрограммныйИнтерфейс

Функция ВерсияПриложения() Экспорт
	
	Возврат "0.8.45";
	
КонецФункции

Функция ИмяМакетаReact() Экспорт
	
	Возврат "ReactПриложение";
	
КонецФункции

// Опционально: URL JSON-манifest; иначе GitHub Releases (сервер 1С) и bin/update-manifest.json.
Функция URLМанифестаОбновлений() Экспорт
	
	Возврат "";
	
КонецФункции

Функция РепозиторийGitHub() Экспорт
	
	Возврат "Nior21/edo-adiutor";
	
КонецФункции

Функция ИмяФайлаGitHubRelease() Экспорт
	
	Возврат "EdoAdiutor.epf";
	
КонецФункции

Функция ИмяФайлаEPF() Экспорт
	
	Возврат "ПомощникДляЭДО.epf";
	
КонецФункции

Функция ПодкаталогРелизов() Экспорт
	
	Возврат "releases";
	
КонецФункции

Функция КлючХранилищаНастроекОбновления() Экспорт
	
	Возврат "ПомощникДляЭДО";
	
КонецФункции

Функция СведенияОВнешнейОбработке() Экспорт
	
	#Если Сервер Тогда
		
		ПараметрыРегистрации = ДополнительныеОтчетыИОбработки.СведенияОВнешнейОбработке();
		ПараметрыРегистрации.Вид = "ДополнительнаяОбработка";
		ПараметрыРегистрации.Версия = ВерсияПриложения();
		ПараметрыРегистрации.Наименование = "Помощник для ЭДО";
		ПараметрыРегистрации.Информация = "Просмотр и аудит электронных перевозочных документов (React UI).";
		ПараметрыРегистрации.БезопасныйРежим = Ложь;
		
		НоваяКоманда = ПараметрыРегистрации.Команды.Добавить();
		НоваяКоманда.Представление = "Помощник для ЭДО";
		НоваяКоманда.Идентификатор = "ОткрытьПомощникЭДО";
		НоваяКоманда.Использование = "ОткрытиеФормы";
		
		Возврат ПараметрыРегистрации;
		
	#Иначе
		
		Возврат Неопределено;
		
	#КонецЕсли
	
КонецФункции

#КонецОбласти







